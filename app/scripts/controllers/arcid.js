'use strict';

/**
 * @ngdoc function
 * @name crnaClientApp.controller:arcidController
 * @description
 * # ArcidCtrl
 * Controller of the crnaClientApp
 */
angular.module('crnaClientApp')
  .controller('arcidController', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
